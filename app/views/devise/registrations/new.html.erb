<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">ユーザー新規登録</h2>

        <%# 「モデル名」にはUserモデルであれば@userを渡しましょう。%>
        <%# 「新規登録機能へのパス」は、devise導入後にrails routesを実行してdevise/registrations#createへのパスを確認し、記載してください。 %>
        <%= form_with model: @user, url: user_registration_path, local: true do |f| %>

        <div class="field">
          <%= f.label :hoge, "メールアドレス" %><br />
          <%= f.email_field :hoge, autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          <%= f.label :hoge, "パスワード（6文字以上）" %><br />
          <%= f.password_field :hoge, autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :hoge, "パスワード再入力" %><br />
          <%= f.password_field :hoge, autocomplete: "new-password" %>
        </div>

        <div class="field">
          <%= f.label :hoge, "ユーザー名" %><br />
          <%= f.text_field :hoge %>
        </div>

        <div class="field">
          <%= f.label :hoge, "アイコン画像" %><br />
          <%= f.file_field :hoge %>
        </div>

        <div class="actions">
          <%= f.submit "新規登録", class: :form__btn  %>
        </div>
      <% end %>
    </div>
  </div>
</div>